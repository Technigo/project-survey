import React, { useState } from 'react'
import { Form } from './Form';

export const Submit = ({ formData, setFormData }) => {

    return(
      <div className="submit-container">
       <h2>Thank you!</h2>
      </div>
    );
  }