import React, { useState } from 'react';

export const Submit = () => {
    const [surveySubmit, setSurveySubmit] = useState;
    /* When this button is pressed it should hide the input form and show a summary of submissions */
    return <button>Submit Survey</button>
}