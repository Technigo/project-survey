import React, { useState } from 'react';

export const Submit = () => {
     /*const [surveySubmit, setSurveySubmit] = useState (false); 
     if (surveySubmit = true) {

     }*/
    /* When this button is pressed it should hide the input form and show a summary of submissions */
    return <button /*onClick={() => setSurveySubmit(surveySubmit = true)}*/>Submit Survey</button>
}

/* Save user input i variables and create function that prints them in a summary*/