import React from 'react';

const Name = ({id, question_text,input_type, options, onChange, userInput}) => {
  return (
 <div>
  <p>Fråga: {id}</p>
  <p>Fråga: {question_text}</p>
  <p>Fråga: {input_type}</p>
 </div>   

  )
}
export default Name;