import React from 'react'

const OrderResult = ({name}) => {

    return(
        <div className="order-container">
            <h1 className="order">Thank you (name) for you order. (amount) issues of Grace Magazine will arrive directly to your snail mailbox. We are so happy to have you as a reader!</h1>
        </div>
    )
}

export default OrderResult