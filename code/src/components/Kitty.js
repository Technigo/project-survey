/* import React from 'react'

export const Kitty = ({ milk, signmeeter, setSignmeeter }) => {
  const handleSignMeeter = (event) => {
    setSignmeeter(event.target.value);
  }
  return (
    <div className="signmeeter">
      <p>How much of a {milk} are you?</p>
      <div key={signmeeter}>

        <input type="range" value={signmeeter} onChange={handleSignMeeter} />

      </div>
      {milk && (
        <p>That much {milk} huh?</p>
      )}
    </div>
  );
} */

/* NR 3 */