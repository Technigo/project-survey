import React from 'react';

const SecondQuestion = (arenaChoice, onArenaChoiceSet, onStepChange) => {
  return (
    <form>
      <h1>Hello</h1>
      <button onClick={onStepChange}>See overview</button>
    </form>
  );
};

export default SecondQuestion;