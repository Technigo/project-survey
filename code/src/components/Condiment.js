import React from 'react';

const Condiments = (props) => {

    const condimentsArray = ['mayo', 'mustard', 'garlicSauce', 'remoulade', 'ketchup', 'tomatoSauce' ]

    const [setCondiment] = [props.setCondiment]
}