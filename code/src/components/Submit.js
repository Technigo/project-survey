import React from "react";

import "./form.css"

export const Submit = () => {

  return (
    <section className="section-container">
      <button
        tabIndex="0"
        type="submit"
      >
        Submit
      </button>
    </section>
  );
};

export default Submit;