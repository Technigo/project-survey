/* import React from 'react';
import { QuizResult } from './QuizResults';

export const Results = ({ name, color, prefer, things }) => {
  const result = QuizResult({ prefer, things, color });

  return (
    <div>
      <section className="inputName">
        <h2>{name}, your favourite animal is {result}</h2>
      </section>
    </div>
  );
}; */