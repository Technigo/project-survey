/*import React, { useState } from "react";

const experienceGroups = ["Only remote", "No, just remote", "No, just from office"];
const App = () => {
  const [experienceGroup, setExperienceGroup] = useState();

  return (
    <form>
      Experience:
      {experienceGroups.map((group) => (
        <label key={group}>
          <input
            type="radio"
            value={group}
            onChange={(event) => setExperienceGroup(event.target.value)}
            checked={ageGroup === group}
          />
          {group}
        </label>
      ))}
    </form>
  );
};

export default Radio;*/