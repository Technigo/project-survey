import React from 'react';

import './header.css';

export const Header = () => {
    return(
        <header tabIndex={'0'} className={'header'}>
            <h1>SURVEY TIME BABY!</h1>
        </header>
    );
};