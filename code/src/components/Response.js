import React, {useState} from 'react'
import './response.css'

export const Response = ({name}) => {

  //const { name } = props;

  return (
    <form>
      <section>
  <h1>{name}</h1>
      </section>
    </form>
  );
};


