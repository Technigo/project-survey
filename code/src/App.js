import React from 'react';
import { Gender } from './Gender';
import { Frequency } from './Frequency';

export const App = () => {
  return (
    <div>
      <Gender />
      <Frequency />
    </div>
  );
};

export default App;
